{
  "id": "neovim",
  "version": "1.0.0",
  "name": "Neovim",
  "description": "Installs Neovim from GitHub releases (prebuilt binaries)",
  "options": {
    "version": {
      "type": "string",
      "default": "stable",
      "description": "Neovim version to install: 'stable', 'nightly', 'latest', or a specific version (e.g. 'v0.10.0')"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
